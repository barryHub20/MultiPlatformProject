#ifndef SHADER_HPP
#define SHADER_HPP
#include "Header.h"

//Universal function prototype: overload with platform appropriate definition
GLuint LoadShaders(const char * vertex_file_path,const char * fragment_file_path);

#endif
